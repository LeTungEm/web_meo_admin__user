export const mutations = {
    setListPublished(state, listPublished) {
        state.listPublished = listPublished;
    },
    // addProduct(state, product) {
    //     let products = state.products;
    //     products.push(product);
    //     state.products = products;
    // },
};